# Roadmap portal (React)

Builds to `./roadmap/app/`. PHP keeps login/SSO and attachment upload/delete. Public pages swap to this shell only after `npm run build` succeeds.

```
cd roadmap-ui
npm install
npm run build
```
