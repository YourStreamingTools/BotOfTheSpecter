# Fresh install: product API host

Public hostname: `https://api.botofthespecter.com`

User-facing facts (health, `/docs`, auth) live in [README.md](./README.md).

OS layout, service user, secret files, firewall, DNS cutover, and systemd units are **operator-only**. Keep that runbook off the public repository.
