{
    "_comment": "OBSOLETE for SSH/hosts. TTS publish uses .env only: WEB-HOST, SSH_USERNAME, SSH_PASSWORD (optional TTS_LOCAL_DIR, TTS_REMOTE_DIR, TTS_PUBLISH_DIR). Kept only as a voice/model note for operators.",
    "tts_model": "gpt-4o-mini-tts",
    "default_voice": "alloy",
    "available_voices": ["alloy", "ash", "ballad", "coral", "echo", "fable", "nova", "onyx", "sage", "shimmer", "verse"]
}
