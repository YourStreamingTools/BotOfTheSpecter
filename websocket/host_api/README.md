# WebSocket host control API

Internal host lifecycle tool. Authenticate with a service-scoped admin key via `X-API-KEY`.

Streamer user keys are not accepted. Do not document unit names, bind addresses, or host paths here.

Dashboard admin uses this service over HTTPS. See `./dashboard/includes/websocket_control_client.php`.
