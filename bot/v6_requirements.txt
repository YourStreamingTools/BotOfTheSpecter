# BotOfTheSpecter Twitch bot v6 (beta-v6.py) — TwitchIO 3.x
# Install into (server): /home/botofthespecter/venvs/v6
#   venvs/v6/bin/pip install -r v6_requirements.txt
#
# Do not install into the stable/beta venvs (those pin TwitchIO 2.10).

# Async Twitch API client (v6 rewrite) — TwitchIO 3.3.x stable
# Docs: https://twitchio.dev/en/stable/
# EventSub models live in `twitchio.eventsub` (not twitchio.ext.eventsub).
twitchio==3.3.2
twitchio[starlette]==3.3.2

# Async HTTP client for Python
aiohttp

# WebSocket libraries
websockets
python-socketio

# For streaming video/audio
streamlink

# Timezones
pytz

# Load environment variables from .env
python-dotenv

# Async MySQL client
aiomysql

# Geo-location tools
geopy

# Unit conversions
pint

# Discord API
discord.py

# Joke API
jokeapi

# jokeapi imports this at runtime but doesn't declare it as an install
# dependency, so pip never pulls it in on its own.
simplejson

# Translation
deep_translator

# For system monitoring
psutil

# For text-to-speech conversion
pyttsx3

# For cryptographic operations
PyNaCl

# For SSH connections
paramiko

# YouTube video downloader
yt_dlp

# OpenAI API client
openai

# JSON query language (botocore / streamlink / yt_dlp)
jmespath>=0.7.1,<2.0.0

# Date and time utilities
python-dateutil>=2.1,<3.0.0

# English hyphenation / syllable splitting (word replace module)
pyphen
